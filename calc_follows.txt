FOLLOW (PROGRAM) = {$}
FOLLOW (FUNCLIST) = {follow(PROGRAM) U follow(FUNCLISTAUX)}
FOLLOW (FUNCLISTAUX) = {follow(FUNCLIST)}
FOLLOW (FUNCDEF) = {fist(FUNCLISTAUX)-& U follow(FUNCLIST)}
FOLLOW (PARAMLIST) = {first(')') U follow(PARAMLISTAUX)}
FOLLOW (PARAMLISTAUX) = {follow(PARAMLIST)}
FOLLOW (DATATYPE) = {first(ident) U first('[')}
FOLLOW (STATEMENT) = {follow(PROGRAM) U first(}) U first(OPT_STATELIST)-& U follow(STATELIST)}
FOLLOW (VARDECL) = {first(;)}
FOLLOW (OPT_VECTOR) = {follow(VARDECL)}
FOLLOW (ATRIBSTAT) = {first(;) U first(')'}
FOLLOW (ATRIBSTAT_RIGHT) = {follow(ATRIBSTAT)}
FOLLOW (FUNCCALL_OR_EXPRESSION) = {follow(ATRIBSTAT_RIGHT)}
FOLLOW (FOLLOW_IDENT) =  {follow(FUNCCALL_OR_EXPRESSION)}
FOLLOW (PARAMLISTCALL) = {fist(')') U follow(PARAMLISTCALLAUX)}
FOLLOW (PARAMLISTCALLAUX) = {follow(PARAMLISTCALL)}
FOLLOW (PRINTSTAT) = {first(;)}
FOLLOW (READSTAT) = {first(;)}
FOLLOW (RETURNSTAT) = {first(;)}
FOLLOW (IFSTAT) = {follow(STATEMENT)}
FOLLOW (OPT_ELSE) = {follow(IFSTAT)}
FOLLOW (FORSTAT) = {follow(STATEMENT)}
FOLLOW (STATELIST) = {first('}') U follow(OPT_STATELIST)}
FOLLOW (OPT_STATELIST) = {follow(STATELIST)}
FOLLOW (ALLOCEXPRESSION) = {follow(ATRIBSTAT_RIGHT)}
FOLLOW (OPT_ALLOC_NUMEXP) = {first(REC_UNARYEXPR)-& U first(REC_PLUS_MINUS_TERM)-& U first(OPT_REL_OP_NUM_EXPR) U follow(FOLLOW_IDENT) U follow(ALLOCEXPRESSION) U follow(LVALUE)}
FOLLOW (EXPRESSION) = {follow(PRINTSTAT) U first(')') U first(;)}
FOLLOW (OPT_REL_OP_NUM_EXPR) = {follow(EXPRESSION) U follow(FUNCCALL_OR_EXPRESSION) U follow(FOLLOW_IDENT)}
FOLLOW (REL_OP) = {first(NUMEXPRESSION)}
FOLLOW (NUMEXPRESSION) = {first(']') U first(OPT_REL_OP_NUM_EXPR)-& U follow(EXPRESSION) U follow(OPT_REL_OP_NUM_EXPR) U first(')')}
FOLLOW (REC_PLUS_MINUS_TERM) = {first(OPT_REL_OP_NUM_EXPR)-& U follow(FUNCCALL_OR_EXPRESSION) U follow(NUMEXPRESSION)}
FOLLOW (PLUS_OR_MINUS) = {first(TERM) U first(FACTOR)}
FOLLOW (TERM) = {first(REC_PLUS_MINUS_TERM)-& U follow(NUMEXPRESSION) U follow(REC_PLUS_MINUS_TERM) U follow(REC_UNARYEXPR)}
FOLLOW (REC_UNARYEXPR) = {first(REC_PLUS_MINUS_TERM) - & U first(OPT_REL_OP_NUM_EXPR) - & U follow(FUNCCALL_OR_EXPRESSION) U follow(TERM)}
FOLLOW (UNARYEXPR_OP) = {first(TERM)}
FOLLOW (UNARYEXPR) = {first(REC_UNARYEXPR)-& U follow(TERM)}
FOLLOW (FACTOR) = {follow(UNARYEXPR) U first(REC_UNARYEXPR)-& U first(REC_PLUS_MINUS_TERM)-& U first(OPT_REL_OP_NUM_EXPR)-& U follow(FUNCCALL_OR_EXPRESSION)}  
FOLLOW (LVALUE) = {first(=) U follow(READSTAT) U follow(FACTOR)}